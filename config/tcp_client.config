[{tcp_client,[
    {adress, "127.0.0.1"},                                  % 连接地址
    {port, 9999},                                           % 连接端口
    {send_timeout, 5000}                                    % 发送超时
    ]
}].
